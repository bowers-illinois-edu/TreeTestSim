% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_data.R
\name{run_synthetic_comparison}
\alias{run_synthetic_comparison}
\title{Run All Testing Methods on a Synthetic Experiment}
\usage{
run_synthetic_comparison(
  idat,
  bdat,
  k,
  max_level,
  tau_size,
  prop_blocks_0 = 0,
  tau_fn = tau_norm,
  nsims = 200L,
  ncores_sim = 1L,
  thealpha = 0.05,
  adaptive_tau = 0.1,
  methods = c("td_unadj", "td_hommel", "td_adaptive", "td_adaptive_hommel", "bu_hommel",
    "bu_bh")
)
}
\arguments{
\item{idat}{Individual-level data.table from
\code{\link{generate_synthetic_experiment}}.}

\item{bdat}{Block-level data.table from
\code{\link{generate_synthetic_experiment}}.}

\item{k}{Integer. Branching factor (must match the data).}

\item{max_level}{Integer. Tree depth (must match the data).}

\item{tau_size}{Numeric. Effect size parameter passed to \code{tau_fn}.}

\item{prop_blocks_0}{Numeric between 0 and 1. Proportion of blocks with zero
treatment effect. When the synthetic data has a known non-null pattern
(from \code{generate_synthetic_experiment}), set to 0 and let the
\code{nonnull} column control which blocks have effects.}

\item{tau_fn}{Function. Treatment effect generator (default
\code{\link{tau_norm}}). See \code{\link{create_effects}}.}

\item{nsims}{Integer. Number of randomization reruns per method (default 200).}

\item{ncores_sim}{Integer. Number of cores for parallel reruns (default 1).}

\item{thealpha}{Numeric. Nominal significance level (default 0.05).}

\item{adaptive_tau}{Numeric. Power threshold for the adaptive alpha method
(default 0.1).}

\item{methods}{Character vector. Which methods to run. Default: all six.}
}
\value{
A data.table with one row per method, containing columns
\code{method} plus all error/power metrics from \code{\link{calc_errs_new}}.
}
\description{
Generates treatment effects on the synthetic data and runs all six testing
methods (four top-down, two bottom-up) via \code{\link{padj_test_fn}}.
}
